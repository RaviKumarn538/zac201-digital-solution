<nav class="app-navbar sticky-top">
  <div class="container nav-shell max-w-7xl px-3 sm:px-4 lg:px-6">
    <div class="nav-content">
      <a class="navbar-brand brand text-lg sm:text-xl" href="/">Zac.Living</a>

      <ul class="navbar-nav main-nav-links">
        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="/rooms">Browse Rooms</a></li>
        <li class="nav-item"><a class="nav-link" href="<%= currentUser && currentUser.role === 'owner' ? '/list-your-property' : '/owner/signup' %>">List your property</a></li>
        <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
        <% if (!currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/dashboard">Student Dashboard</a></li>
          <li class="nav-item mobile-only-auth"><a class="nav-link nav-login-chip" href="/login">Login</a></li>
          <li class="nav-item mobile-only-auth"><a class="nav-link nav-signup-chip" href="/signup">Sign up</a></li>
        <% } %>
        <% if (currentUser && currentUser.role === "student") { %>
          <li class="nav-item"><a class="nav-link" href="/student/dashboard">Student Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">My Profile</a></li>
        <% } %>
        <% if (currentUser && currentUser.role === "owner") { %>
          <li class="nav-item"><a class="nav-link" href="/owner/dashboard">Owner Dashboard</a></li>
        <% } %>
        <% if (currentUser && currentUser.role === "admin") { %>
          <li class="nav-item"><a class="nav-link" href="/admin">Admin Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/rooms/new">Add Listing</a></li>
        <% } %>
      </ul>

      <div class="nav-actions <%= currentUser ? 'is-user' : 'is-guest' %>">
        <% if (currentUser) { %>
          <span class="user-pill"><%= currentUser.role === "admin" ? "Admin" : currentUser.role === "owner" ? "Owner" : "Student" %></span>
          <form method="post" action="/logout" class="m-0">
          <button class="btn btn-outline-primary btn-sm nav-auth-link nav-logout" type="submit">Logout</button>
        </form>
      <% } else { %>
          <a href="/login" class="btn btn-outline-primary btn-sm nav-auth-link nav-login">Login</a>
          <a href="/signup" class="btn btn-primary btn-sm nav-auth-link nav-signup">Sign up</a>
      <% } %>
      </div>
    </div>
  </div>
</nav>
